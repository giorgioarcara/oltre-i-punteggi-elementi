setwd("~/Documents/Lavori Unipd/Lezioni Corsi - docenze/Slides-Metodi-Statistici-Neuropsi-Forense")
ls appendices
quarto render
12000 * 10
6 * 200
2500 * 6 * 12
2500 * 6 * 10
# Parameters
x_obs <- -1.64       # observed z-score
r_xx <- 0.64         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.64       # observed z-score
r_xx <- 0.99         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.64       # observed z-score
r_xx <- 0.99         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.64       # observed z-score
r_xx <- 0.9999         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.65       # observed z-score
r_xx <- 0.9999         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.66       # observed z-score
r_xx <- 0.9999         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.9999         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.9999         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.66         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.66         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.66         # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.99        # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
x <- -1.64
r <- 0.64
prob_T_le_x <- pnorm(sqrt(1 - r) * x)   # = Phi(sqrt(1-r) * x)
prob_T_le_x
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.95        # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
# Parameters
x_obs <- -1.68       # observed z-score
x <- -1.68
r <- 0.64
prob_T_le_x <- pnorm(sqrt(1 - r) * x)   # = Phi(sqrt(1-r) * x)
prob_T_le_x
t_mean
t_sd
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.95        # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that t
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.95        # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
x <- -1.68
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.95        # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
x <- -1.68
r <- 0.64
prob_T_le_x <- pnorm(sqrt(1 - r) * x)   # = Phi(sqrt(1-r) * x)
prob_T_le_x
# Parameters
x_obs <- -1.68       # observed z-score
r_xx <- 0.95        # reliability
t_thresh <- -1.64    # true-score threshold (z-score)
sigma_T <- 1         # standard deviation of true scores (z-scale)
mu_T <- 0            # mean of true scores (z-scale)
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Probability that true score is below threshold
prob_below <- pnorm(t_thresh, mean = t_mean, sd = t_sd)
cat("Probability that true score < threshold:", round(prob_below, 4), "\n")
x_obs
mu_T
x_obs
x <- -1.68
r <- 0.64
t_mean
t_sd
x <- -1.68
r <- 0.64
prob_T_le_x <- pnorm(sqrt(1 - r) * x)   # = Phi(sqrt(1-r) * x)
prob_T_le_x
x
t_sd
t_mean
t_sd
sigma_T
t_sd <- sigma_T * sqrt(1 - r_xx)               # SD[T | X=x]
# Conditional mean and standard deviation of true score
t_mean <- mu_T + r_xx * (x_obs - mu_T)          # E[T | X=x]
prob_T_le_x <- pnorm(sqrt(1 - r) * x)   # = Phi(sqrt(1-r) * x)
prob_T_le_x
r <- 0.54
prob_T_le_x <- pnorm(sqrt(1 - r) * x)   # = Phi(sqrt(1-r) * x)
prob_T_le_x
rm(list=ls())
library(readxl)
library(openxlsx)
# NOTE the years is the past
Lab_dat = as.data.frame(read_excel("Original Data/Lab_ricercatori_2024.xlsx"))
setwd("~/Documents/Projects/Book-Psychometrics and Statistics for Clinical Neuropsychology/oltre-i-punteggi-elementi")
0.2*38
0.2*32
0.2*38
38/6.2
6.2/38
